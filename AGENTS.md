# Workspace Rules

- Use `document-management-agent` for document triage, naming, archive, and retrieval workflows.
- Use `gmail` for live Gmail operations.
- Use `google-drive` for live Google Drive operations.
- Search before downloading.
- Keep secrets in env or OpenClaw config, never in prompt text.
- Do not rename, move, or mark messages as processed until the classification is confident.
